import type { PublicationType,SecretaryDocumentType } from '@/lib/domain/secretary-templates';import { createSafeDraft } from '@/lib/domain/secretary-templates';
export interface CommunicationGenerationInput{organizationId:string;type:SecretaryDocumentType|PublicationType;prompt:string;brand?:{primaryColor?:string;logoUrl?:string}}
export interface CommunicationAIProvider{generateDocument(input:CommunicationGenerationInput):Promise<{title:string;body:string}>;generateVisual(input:CommunicationGenerationInput):Promise<{editableProject:Record<string,unknown>;previewUrl?:string}>}
export function getCommunicationAIProvider():CommunicationAIProvider|null{return null}
export async function generateCommunicationDraft(input:CommunicationGenerationInput){const provider=getCommunicationAIProvider();if(!provider)return {...createSafeDraft(input.prompt,input.type),provider:'SAFE_TEMPLATE_FALLBACK',error:'COMMUNICATION_PROVIDER_NOT_CONFIGURED'};if(['FLYER','POSTER','GREETING_CARD','DIGITAL_INVITATION','BANNER','VISUAL_ANNOUNCEMENT'].includes(input.type)){return {...await provider.generateVisual(input),status:'DRAFT' as const,requiresReview:true,provider:'CONFIGURED_AI'}}return {...await provider.generateDocument(input),status:'DRAFT' as const,requiresReview:true,provider:'CONFIGURED_AI'}}
