export type Sensitivity='PUBLIC'|'INTERNAL'|'PERSONAL'|'SENSITIVE'|'RESTRICTED';export type ModelClass='L0_DETERMINISTIC'|'L1_SIMPLE'|'L2_ECONOMY'|'L3_ESCALATION';
export function classifySensitivity(value:string){const q=value.toLowerCase();if(/passport|paspor|verification evidence|moderation evidence|reporter|secret|token|password|private organisation|cross.entity/.test(q))return 'RESTRICTED' as Sensitivity;if(/immigration case|financial account|document|private/.test(q))return 'SENSITIVE' as Sensitivity;return 'PUBLIC' as Sensitivity}
export function routeModel(input:{sensitivity:Sensitivity;highRisk:boolean;hasAuthoritativeSource:boolean;complex?:boolean}){if(input.hasAuthoritativeSource&&!input.complex)return {modelClass:'L0_DETERMINISTIC' as ModelClass,provider:'duta'};if(input.sensitivity==='RESTRICTED'||input.sensitivity==='SENSITIVE')return {modelClass:'L3_ESCALATION' as ModelClass,provider:'openai'};if(!input.highRisk)return {modelClass:'L1_SIMPLE' as ModelClass,provider:'gemini'};return {modelClass:'L2_ECONOMY' as ModelClass,provider:'groq'}}
export const usageWeight:Record<ModelClass,number>={L0_DETERMINISTIC:0,L1_SIMPLE:1,L2_ECONOMY:2,L3_ESCALATION:4};export function aiEnabled(){return process.env.NODE_ENV==='production'?process.env.DUTA_AI_ENABLED==='true':process.env.DUTA_AI_ENABLED!=='false'}
export function isAllowedOpenAIModel(model:string|undefined){return model===undefined||model==='gpt-5.6-luna'}
