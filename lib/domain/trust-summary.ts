export type TrustSummaryInput = { identityVerified: boolean; entityRegistrationChecked: boolean; commercialEligibility: 'approved'|'other'; employerEligibility: 'approved'|'other'; officialSource?: string; moderationStatus: 'NORMAL'|'LIMITED'|'UNDER_REVIEW'|'SUSPENDED'|'BANNED'|'REGULATORY_HOLD'|'CLOSED'; lastChecked?: Date };
export function getTrustSummary(input: TrustSummaryInput) { return { identity: input.identityVerified ? 'CHECKED' : 'NOT_CHECKED', entityRegistration: input.entityRegistrationChecked ? 'CHECKED' : 'NOT_CHECKED', commercialEligibility: input.commercialEligibility === 'approved' ? 'CURRENT' : 'NOT_CURRENT', employerEligibility: input.employerEligibility === 'approved' ? 'CURRENT' : 'NOT_CURRENT', officialSource: input.officialSource ?? 'NOT_APPLICABLE', moderation: input.moderationStatus === 'NORMAL' ? 'NO_CURRENT_RESTRICTION' : input.moderationStatus, lastChecked: input.lastChecked?.toISOString(), disclaimer: 'Periksa sebelum percaya. Maklumat ini ialah ringkasan fakta, bukan jaminan keselamatan atau kelulusan kerajaan.' }; }
