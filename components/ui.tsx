import { AlertCircle,CheckCircle2,ExternalLink,LoaderCircle,ShieldCheck } from 'lucide-react';
export function PageHeader({eyebrow,title,description,action}:{eyebrow?:string,title:string,description?:string,action?:React.ReactNode}){return <header className="page-header"><div>{eyebrow&&<span className="eyebrow">{eyebrow}</span>}<h1>{title}</h1>{description&&<p>{description}</p>}</div>{action}</header>}
export function DemoBadge(){return <span className="badge demo">DEMO DATA</span>}
export function TrustBadge({level='official'}:{level?:'official'|'community'|'unverified'|'member'}){const map={official:['Sumber Resmi',ShieldCheck],community:['Terverifikasi Komunitas',CheckCircle2],unverified:['Belum Diverifikasi',AlertCircle],member:['Member Seller',CheckCircle2]} as const;const [label,Icon]=map[level];return <span className={`badge ${level}`}><Icon size={13}/>{label}</span>}
export function SourceMeta({name,date,url}:{name:string,date:string,url:string}){return <div className="source-meta"><div><span>Sumber</span><b>{name}</b></div><div><span>Terakhir diperiksa</span><b>{new Date(date+'T00:00:00').toLocaleDateString('id-ID',{day:'numeric',month:'short',year:'numeric'})}</b></div><a href={url} target="_blank" rel="noreferrer">Buka sumber <ExternalLink size={14}/></a></div>}
export function EmptyState({title='Data belum tersedia.',description='Belum ada data yang dapat ditampilkan.'}:{title?:string,description?:string}){return <div className="empty"><AlertCircle/><b>{title}</b><p>{description}</p></div>}
export function LoadingState(){return <div className="loading"><LoaderCircle className="spin"/>Memuat data…</div>}
