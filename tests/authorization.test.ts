import { describe,expect,it } from 'vitest';import { canInOrganization,hasRole } from '../lib/auth/authorization';describe('authorization',()=>{it('prevents member from viewing finance',()=>expect(canInOrganization('MEMBER','finance.view')).toBe(false));it('allows treasurer finance management',()=>expect(canInOrganization('TREASURER','finance.manage')).toBe(true));it('prevents secretary from managing finance',()=>expect(canInOrganization('SECRETARY','finance.manage')).toBe(false));it('respects system role hierarchy',()=>{expect(hasRole('SUPER_ADMIN','MODERATOR')).toBe(true);expect(hasRole('USER','MODERATOR')).toBe(false)})});
