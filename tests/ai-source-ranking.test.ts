import { describe, expect, it } from 'vitest';
import { rankSources, type RankedSource } from '../lib/services/ai-source-ranking';
const sources: RankedSource[]=[{id:'4',label:'model',tier:'TIER_4'},{id:'3',label:'community',tier:'TIER_3'},{id:'2',label:'partner',tier:'TIER_2',verifiedPartner:true},{id:'1',label:'official',tier:'TIER_1',official:true},{id:'paid',label:'sponsored',tier:'TIER_3',sponsored:true}];
describe('AI source ranking',()=>{it('ranks authority without commercial upgrades',()=>{expect(rankSources(sources,'NONE').sources.map(x=>x.tier)).toEqual(['TIER_1','TIER_2','TIER_3','TIER_3','TIER_4']);expect(sources.find(x=>x.id==='paid')?.tier).toBe('TIER_3')});it('requires tier 1 when official evidence is required',()=>{expect(rankSources(sources,'OFFICIAL_REQUIRED')).toMatchObject({status:'ok',sources:[{id:'1'}]});expect(rankSources(sources.filter(x=>x.tier!=='TIER_1'),'OFFICIAL_REQUIRED')).toMatchObject({status:'AUTHORITATIVE_SOURCE_REQUIRED',sources:[]})});it('permits Tier 1 and Tier 2 for trusted requests',()=>expect(rankSources(sources,'TRUSTED').sources.map(x=>x.tier)).toEqual(['TIER_1','TIER_2']))});
