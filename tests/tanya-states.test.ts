import { readFileSync } from 'node:fs';import { describe,expect,it } from 'vitest';
const chat=readFileSync('components/ai-chat.tsx','utf8');
describe('Tanya DUTA presentation states',()=>{it('maps the real voice lifecycle without treating a transcript as an answer',()=>{for(const state of ['READY','CAPTURING','TRANSCRIBING','TRANSCRIPT','REQUESTING_ANSWER','ANSWER_RECEIVED','CONTROLLED_REJECTION','PROVIDER_UNAVAILABLE','ERROR'])expect(chat).toContain(state);expect(chat).toContain("setVoiceState('TRANSCRIPT');await ask(next,true)");expect(chat).toContain("setVoiceState('ANSWER_RECEIVED')")});it('sends transcript through the supported text contract and keeps source metadata returned-only',()=>{expect(chat).toContain("channel:'text'");expect(chat).not.toContain("location:'Kuala Lumpur'");expect(chat).not.toContain('16 Agu 2026');expect(chat).toContain('source.lastChecked')});it('keeps microphone and retry controls honest and accessible',()=>{expect(chat).toContain("aria-label={voiceState==='CAPTURING'?'Berhenti merekam':'Mulai bicara'}");expect(chat).toContain('role="status" aria-live="polite"');expect(chat).toContain('canRetry&&<button')});});
