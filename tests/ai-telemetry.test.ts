import { describe, expect, it } from 'vitest';
import { buildAiTelemetry } from '../lib/services/ai-telemetry';
const base={userRef:'00000000-0000-4000-8000-000000000001',intent:'GENERAL',risk:'low',sensitivity:'PUBLIC' as const,provider:'gemini',model:'server-model',sourceRequirement:'NONE' as const,sourceTier:'TIER_3' as const,quotaOutcome:'allowed' as const,weightedUnits:1,latencyMs:3,success:true,errorCode:null,fallbackUsed:false};
describe('AI telemetry privacy',()=>{it('records metadata and never retains a prompt marker',()=>{const event=buildAiTelemetry({...base,modelClass:'L1_SIMPLE',input:'PRIVATE_MARKER_12345 passport narrative',outputTokens:12});expect(JSON.stringify(event)).not.toContain('PRIVATE_MARKER_12345');expect(event.inputTokens).toBeGreaterThan(0)});it('records L0 as zero-cost and provider-free',()=>{const event=buildAiTelemetry({...base,modelClass:'L0_DETERMINISTIC',provider:null,model:null,weightedUnits:0,quotaOutcome:'not_applicable',input:'a'});expect(event.estimatedCostUsd).toBe(0);expect(event.provider).toBeNull()});it('caps server output estimate',()=>expect(buildAiTelemetry({...base,modelClass:'L3_ESCALATION',input:'a',outputTokens:999}).outputTokens).toBe(300))});
