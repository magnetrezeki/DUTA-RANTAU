import { describe,expect,it } from 'vitest';import { officialSources } from '../lib/demo-data';describe('official source integrity',()=>{it('contains all 27 approved channels',()=>expect(officialSources).toHaveLength(27));it('uses HTTPS only',()=>expect(officialSources.every(x=>x.url.startsWith('https://'))).toBe(true));it('exposes checked date and verified status',()=>expect(officialSources.every(x=>x.lastChecked==='2026-08-16'&&x.status==='VERIFIED')).toBe(true));it('has no invented operational fields',()=>expect(officialSources.some(x=>'phone' in x||'address' in x||'hours' in x||'fees' in x)).toBe(false))});

