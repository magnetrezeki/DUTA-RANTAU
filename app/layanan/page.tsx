import { officialSources } from '@/lib/demo-data';import { PageHeader,SourceMeta,TrustBadge } from '@/components/ui';import { SearchFilter } from '@/components/search-filter';import { Building2,FileText,ShieldAlert,GraduationCap,BriefcaseBusiness,Plane } from 'lucide-react';const institutions=[...new Set(officialSources.filter(s=>['Situs resmi','Instagram'].includes(s.channel)).map(s=>s.institution))];const cats=[[FileText,'Paspor & dokumen'],[ShieldAlert,'Perlindungan WNI'],[BriefcaseBusiness,'Ketenagakerjaan'],[GraduationCap,'Pendidikan'],[Plane,'Perjalanan'],[Building2,'Kantor perwakilan']] as const;export const metadata={title:'Layanan RI'};export default function Page(){return <div className="page"><PageHeader eyebrow="SUMBER RESMI" title="Layanan Republik Indonesia" description="Temukan kanal resmi perwakilan RI. DUTA tidak menebak alamat, kontak, jam layanan, biaya, atau persyaratan."/><div className="notice official"><ShieldAlert/><div><b>Informasi sumber terverifikasi, bukan detail prosedur</b><p>Dokumen sumber yang diberikan memverifikasi identitas kanal. Detail operasional wajib diperiksa langsung pada sumber.</p></div></div><div className="category-row">{cats.map(([Icon,x])=><button key={x}><Icon/>{x}</button>)}</div><SearchFilter placeholder="Cari institusi atau layanan…"/><div className="cards-list">{institutions.map(name=>{const src=officialSources.find(s=>s.institution===name&&s.channel==='Situs resmi')??officialSources.find(s=>s.institution===name)!;return <article className="source-card" key={name}><div className="source-icon"><Building2/></div><div className="source-content"><TrustBadge/><h2>{name}</h2><p>{src.category}</p><SourceMeta name={`${name} — ${src.channel}`} date={src.lastChecked} url={src.url}/></div></article>})}</div></div>}

