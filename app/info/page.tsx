import Link from 'next/link';import { ArrowUpRight } from 'lucide-react';
import { infoCategories, malaysiaMissions } from '@/lib/mission-registry';
export const metadata={title:'Info Rantau'};
export default function Page(){return <div className="vp-page vp-wide"><header className="vp-top"><span className="vp-eyebrow">Maklumat · Info Rantau</span><h1>Informasi yang dapat<br/><em>Anda periksa.</em></h1><p className="vp-lead">Enam misi RI dan sumber berwenang lain, dipisahkan menurut tujuan dan provenance.</p></header><section className="vp-section"><h2>Kategori berguna</h2><div className="vp-grid">{infoCategories.map(([title,text])=><article className="vp-empty" key={title}><strong>{title}</strong><p>{text}</p>{title==='Hidup di Malaysia'?<Link href="/info/tempat-wisata">Tempat Wisata <ArrowUpRight size={14}/></Link>:null}</article>)}</div></section><section className="vp-section"><h2>Update resmi dari enam misi</h2><p>DUTA tidak mengaku melakukan auto-ingestion. Tautan ini ialah indeks terkurasi ke kanal resmi; judul atau jumlah berita tidak direka.</p>{malaysiaMissions.map(m=><div className="vp-row" key={m.id}><span><strong>{m.name}</strong><small>NEWS_INFORMATION · diperiksa {m.lastVerified}</small></span><div className="vp-actions"><a href={m.newsUrl} target="_blank" rel="noreferrer">Berita resmi <ArrowUpRight size={15}/></a><a href={m.website} target="_blank" rel="noreferrer">Profil misi <ArrowUpRight size={15}/></a></div></div>)}</section><section className="vp-note"><strong>Belum ada artikel terverifikasi untuk diterbitkan.</strong><p>DUTA tidak membuat suapan berita atau rekomendasi tanpa provenance. Curated intake tetap menunjuk URL kanonik sumber.</p></section><section className="vp-section"><h2>Kanal sosial resmi yang telah dikenal proyek</h2>{malaysiaMissions.map(m=><details className="vp-disclosure" key={m.id}><summary>{m.name}</summary><div className="vp-actions">{Object.entries(m.socials).map(([platform,url])=><a className="vp-outline" href={url} target="_blank" rel="noreferrer" key={platform}>{platform} <ArrowUpRight size={14}/></a>)}</div><p className="vp-muted">Kanal sosial dibuka di platform asal. Tidak ada feed otomatis atau salinan posting yang diklaim oleh DUTA.</p></details>)}</section></div>}
