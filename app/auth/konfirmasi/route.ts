import { NextRequest,NextResponse } from 'next/server';import type { EmailOtpType } from '@supabase/supabase-js';import { createSupabaseServerClient } from '@/lib/supabase/server';export async function GET(req:NextRequest){const url=new URL(req.url);const code=url.searchParams.get('code');const tokenHash=url.searchParams.get('token_hash');const type=url.searchParams.get('type') as EmailOtpType|null;const next=new URL('/profil',url.origin);try{const supabase=await createSupabaseServerClient();if(code){const {error}=await supabase.auth.exchangeCodeForSession(code);if(error)throw error}else if(tokenHash&&type){const {error}=await supabase.auth.verifyOtp({token_hash:tokenHash,type});if(error)throw error}else throw new Error('missing confirmation token');next.searchParams.set('confirmed','1');return NextResponse.redirect(next)}catch{const failed=new URL('/masuk',url.origin);failed.searchParams.set('error','confirmation');return NextResponse.redirect(failed)}}

