import Link from 'next/link';import { AuthForm } from '@/components/auth-form';import { Shield } from 'lucide-react';export const metadata={title:'Masuk'};export default function Page(){return <div className="page auth-page"><section><span className="eyebrow">SELAMAT DATANG KEMBALI</span><h1>Masuk ke DUTA RANTAU</h1><p>Lanjutkan menjelajah, terhubung, dan mengelola aktivitas Anda.</p><AuthForm mode="login"/><p className="auth-switch">Belum punya akun? <Link href="/daftar">Daftar gratis</Link></p><div className="notice"><Shield/><span>Preview membutuhkan PostgreSQL untuk autentikasi persisten.</span></div></section></div>}
