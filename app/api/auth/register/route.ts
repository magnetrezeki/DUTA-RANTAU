import { NextRequest,NextResponse } from 'next/server';import { z } from 'zod';import { createSupabaseServerClient } from '@/lib/supabase/server';import { validatePassword } from '@/lib/auth/password';import { rateLimit } from '@/lib/rate-limit';
const input=z.object({name:z.string().trim().min(2).max(100),email:z.string().email().transform(v=>v.toLowerCase()),password:z.string().max(128),city:z.string().trim().max(100).optional()});
export async function POST(req:NextRequest){const ip=req.headers.get('x-forwarded-for')??'local';if(!rateLimit(`register:${ip}`,5,3600000).ok)return NextResponse.json({error:'Terlalu banyak percobaan.'},{status:429});try{const data=input.parse(await req.json());if(!validatePassword(data.password))return NextResponse.json({error:'Kata sandi minimal 10 karakter dan harus memuat huruf serta angka.'},{status:400});const supabase=await createSupabaseServerClient();const origin=req.headers.get('origin')??new URL(req.url).origin;const {data:result,error}=await supabase.auth.signUp({email:data.email,password:data.password,options:{data:{name:data.name,city:data.city??null},emailRedirectTo:`${origin}/auth/konfirmasi`}});if(error)return NextResponse.json({error:error.message.includes('already')?'Email sudah terdaftar.':'Pendaftaran gagal. Periksa data Anda.'},{status:400});return NextResponse.json({user:result.user?{id:result.user.id,name:data.name}:null,emailConfirmationRequired:!result.session},{status:201});}catch{return NextResponse.json({error:'Data pendaftaran tidak valid.'},{status:400});}}

