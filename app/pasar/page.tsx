import { demoProducts } from '@/lib/demo-data';import { DemoBadge,PageHeader,TrustBadge } from '@/components/ui';import { SearchFilter } from '@/components/search-filter';import { MapPin,Plus,ShoppingBag,Utensils,Palette,Package } from 'lucide-react';const icons=[Utensils,Palette,Package];export const metadata={title:'Pasar Rantau'};export default function Page(){return <div className="page"><PageHeader eyebrow="DARI KOMUNITAS, UNTUK KOMUNITAS" title="Pasar Rantau" description="Temukan produk dan jasa dari warga Indonesia di Malaysia." action={<button className="primary"><Plus/>Mulai berjualan</button>}/><SearchFilter placeholder="Cari makanan, produk, atau jasa…"/><div className="cards-grid market">{demoProducts.map((x,i)=>{const Icon=icons[i];return <article className="product-card" key={x.id}><div className={`product-image p${i+1}`}><Icon/><DemoBadge/></div><div className="card-body"><span className="category">{x.category}</span><h2>{x.name}</h2><p className="seller">{x.seller}</p><p><MapPin/>{x.location}</p><div className="card-foot"><b>{x.price}</b><TrustBadge level={x.trust==='MEMBER_SELLER'?'member':'unverified'}/></div></div></article>})}</div></div>}
