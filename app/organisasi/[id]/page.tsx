import Link from 'next/link';import { ArrowLeft,Building2,Lock,MapPin,ShieldCheck } from 'lucide-react';import { notFound } from 'next/navigation';import { getOrganization } from '@/lib/services/organizations';import styles from '@/components/rantau-world.module.css';export const dynamic='force-dynamic';export async function generateMetadata({params}:{params:Promise<{id:string}>}){const {id}=await params;const org=await getOrganization(id);return {title:org?.name??'Organisasi'}}export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const org=await getOrganization(id);if(!org||org.recordStatus!=='ACTIVE')notFound();const location=[org.city,org.state].filter(Boolean).join(', ');return <div className="page"><Link href="/organisasi" className="back"><ArrowLeft size={16}/>Kembali ke organisasi</Link><header className="page-header"><span className="eyebrow">ORGANISASI · KONTEX</span><h1>{org.name}</h1><p>{org.type}{location?` · ${location}`:''}</p></header><section className={styles.panel}><Building2 size={25}/><b>Yang dapat diketahui dari catatan ini.</b><p>{org.description||'Deskripsi organisasi belum tersedia.'} DUTA tidak menambah status legal, pengesahan, atau kewenangan perwakilan di luar catatan yang tersedia.</p></section><section className={styles.section}><h2>Ruang organisasi belum dibuka dari halaman ini.</h2><p>Anggota, agenda, dokumen, keuangan, dan alat pengelolaan hanya tersedia melalui peran serta permission yang sesuai—bukan dari discovery publik.</p><div className={styles.panel}><Lock size={25}/><b>Data dan tindakan organisasi dilindungi.</b><p>Halaman ini tidak menampilkan anggota, statistik, akses aktif, maupun paket yang tidak dapat dibuktikan atau diakses pengguna saat ini.</p></div></section><section className={styles.panel}><ShieldCheck size={25}/><b>Trust tidak dipindahkan secara otomatis.</b><p>Keberadaan organisasi tidak membuktikan status legal, partner, atau wakil organisasi bagi siapa pun. Periksa konteks dan sumber yang berlaku sebelum bertindak.</p><div className={styles.actions}><Link href="/organisasi">Kembali ke daftar organisasi</Link>{location&&<Link href="/komunitas"><MapPin size={15}/>Jelajahi Rantau</Link>}</div></section></div>}
