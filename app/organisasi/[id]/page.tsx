import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { notFound } from 'next/navigation';
import { getOrganization } from '@/lib/services/organizations';
export const dynamic='force-dynamic';
export async function generateMetadata({params}:{params:Promise<{id:string}>}){const {id}=await params;const org=await getOrganization(id);return {title:org?.name??'Organisasi'}}
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const org=await getOrganization(id);if(!org||org.recordStatus!=='ACTIVE')notFound();const location=[org.city,org.state].filter(Boolean).join(', ');return <div className="vp-page vp-narrow"><Link className="vp-quiet" href="/organisasi">← Kembali ke organisasi</Link><header className="vp-top"><span className="vp-eyebrow">Rantau · Organisasi</span><h1>{org.name}</h1><p className="vp-lead">{org.type}{location?` · ${location}`:''}</p></header><section className="vp-section"><h2>Yang dapat diketahui</h2><p>{org.description||'Deskripsi organisasi belum tersedia.'}</p><div className="vp-note">DUTA tidak menambah status legal, pengesahan, atau kewenangan perwakilan di luar catatan yang tersedia.</div></section><section className="vp-section"><h2>Ruang organisasi</h2><div className="vp-empty"><strong>Pengelolaan tidak tersedia dari halaman publik.</strong><p>Anggota, agenda, dokumen, dan keuangan hanya tersedia melalui peran serta izin yang sesuai. Tidak ada jumlah atau status yang dibuat-buat.</p></div></section><footer className="vp-footer"><Link href="/organisasi">Kembali ke daftar <ArrowUpRight size={16}/></Link></footer></div>}
