import Link from 'next/link';import { Check,ShieldCheck,Sparkles } from 'lucide-react';import { organizationPlans } from '@/lib/domain/organization-plans';export const metadata={title:'Paket DUTA Organisasi'};export default function Page(){return <div className="page org-pricing"><header className="pricing-head"><span className="eyebrow">KANTOR DIGITAL UNTUK SEMUA</span><h1>Pilih paket organisasi</h1><p>Mulai gratis, lalu tingkatkan saat organisasi membutuhkan administrasi, staf virtual, publikasi, dan operasional lanjutan.</p></header><div className="pricing-grid">{organizationPlans.map(plan=><article key={plan.id} className={plan.featured?'featured':''}>{plan.featured&&<span className="popular">PALING SESUAI</span>}<span className="plan-name">{plan.name}</span><div className="org-price"><b>RM{plan.priceMyr.toFixed(plan.priceMyr?2:0)}</b>{plan.period==='month'&&<span>/bulan</span>}</div><p>{plan.description}</p><Link href="/organisasi" className={plan.featured?'primary':'plan-button'}>{plan.id==='FREE'?'Mulai gratis':'Pilih paket'}</Link><ul>{plan.features.map(x=><li key={x}><Check/>{x}</li>)}</ul></article>)}</div><section className="pricing-note"><ShieldCheck/><div><b>Harga dan manfaat ditampilkan secara transparan</b><p>Aktivasi berbayar hanya dilakukan setelah provider pembayaran dikonfigurasi. Tidak ada biaya yang diproses pada mode preview.</p></div></section><section className="virtual-staff"><Sparkles/><div><span>ORGANISASI+ & PRO</span><h2>Staf virtual untuk administrasi dan komunikasi</h2><p>AI Sekretaris, AI Bendahara, pembuatan dokumen, publikasi visual, serta workflow review dan persetujuan.</p></div></section></div>}

